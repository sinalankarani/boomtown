const styles = theme => {
  return {
    shareContainer: {
      height: "100vh",
      padding: 100,
      display: "flex",
      justifyContent: "space-between",
      alignContent: "center"
    }
  };
};

export default styles;
