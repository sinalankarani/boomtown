const styles = theme => {
  return {
    shareContainer: {
      height: "100vh",
      display: "flex",
      justifyContent: "space-evenly",
      paddingTop: 100
    }
  };
};

export default styles;
