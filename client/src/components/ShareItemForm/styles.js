const styles = theme => {
  return {
    formContainer: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "space-around",
      height: 450,
      maxWidth: "60%"
    },
    tagsCheckbox: {
      display: "flex",
      justifyContent: "space-between"
    }
  };
};

export default styles;
