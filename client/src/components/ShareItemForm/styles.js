const styles = theme => {
  return {
    formContainer: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "space-around",
      height: 450
    },
    tagsCheckbox: {
      display: "flex",
      justifyContent: "space-around"
    }
  };
};

export default styles;
