const styles = theme => {
  return {
    itemGrid: {
      background: theme.palette.secondary.main,
      padding: 80,
      maxWidth: "100%",
      margin: "0 auto",
      display: "flex"
    }
  };
};

export default styles;
