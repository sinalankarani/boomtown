const styles = theme => {
  return {
    itemGrid: {
      background: theme.palette.secondary.main,
      padding: 20,
      maxWidth: "100%",
      margin: "0 auto",
      display: "flex"
    }
  };
};

export default styles;
