const styles = theme => {
  return {
    itemGrid: {
      background: theme.palette.secondary.main,
      marginTop: 50,
      padding: 20,
      maxWidth: "100%",
      margin: "0 auto",
      display: "flex"
    }
  };
};

export default styles;
